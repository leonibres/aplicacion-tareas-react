import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Importación del archivo de estilos CSS global
import App from './App'; // Importación del componente App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
